[project]
name = "memorable-password-generation"
version = "0.1.0"
description = "A tool to create personalized and memorable passwords."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "flask>=3.1.3",
]
